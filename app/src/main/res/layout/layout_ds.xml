<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:skin="http://schemas.android.com/android/skin"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <LinearLayout
        android:id="@+id/llyt_dsl"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:gravity="center"
        android:orientation="vertical"
        android:visibility="gone">

        <View
            android:id="@+id/v_dsl_t"
            android:layout_width="wrap_content"
            android:layout_height="0dp" />

        <ImageView
            android:id="@+id/iv_dsl_icon"
            android:layout_width="61dp"
            android:layout_height="61dp"
            android:background="@color/color_main"
            android:src="@drawable/ic_album"
            skin:enable="true" />

        <TextView
            android:id="@+id/tv_dsl_desc"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="12dp"
            android:gravity="center"
            android:text="暂无歌曲"
            android:textColor="@color/color_main"
            android:textSize="@dimen/dimen_text_sub"
            skin:enable="true" />

        <Button
            android:id="@+id/btn_dsl"
            android:layout_width="145dp"
            android:layout_height="45dp"
            android:layout_marginTop="18dp"
            android:gravity="center"
            android:text="重试"
            android:textColor="@color/color_main"
            android:textSize="@dimen/dimen_text_sub"
            android:visibility="gone"
            skin:enable="true" />

        <View
            android:id="@+id/v_dsl_b"
            android:layout_width="wrap_content"
            android:layout_height="0dp" />
    </LinearLayout>

    <com.d.music.view.loading.LoadingLayout
        android:id="@+id/ldl_loading"
        android:layout_width="match_parent"
        android:layout_height="match_parent" />
</FrameLayout>
